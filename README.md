# IPv6-Crawler
The migration to IPv6 has created a significant "visibility gap" in Internet research, as its vast address space necessitates millions of years for traditional
scanning techniques, rendering them ineffective for discovering and analyzing the new Web infrastructure without intelligent and predictive discovery.

# To solve the problem: [remote: Invalid username or token. Password authentication is not supported for Git operations.]

Visit: https://github.com/settings/tokens  [Here, click generate new token (classic)]

run: git push
Enter user name: eg: omkarbhattarai71
For password, enter the token generated or paste it 

To make it permanent run:
git config --global credential.helper store

and run: git push
and paste the token again.


